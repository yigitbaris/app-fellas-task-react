const NavbarTabs = ({ text }) => {
  return (
    <button className=' flex items-center justify-center border h-[35px] px-5 rounded-md font-semibold '>
      {text}
    </button>
  )
}
export default NavbarTabs
